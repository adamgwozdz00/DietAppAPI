CREATE TABLE FOOD
(
    id            VARCHAR(255) PRIMARY KEY,
    name          VARCHAR(255),
    weight        DECIMAL,
    calories      DECIMAL,
    protein       DECIMAL,
    fat           DECIMAL,
    carbohydrates DECIMAL
);
